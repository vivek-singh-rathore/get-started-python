language: python
python:
  - "3.6"
notifications:
  slack: ibm-research:bLcRT4dofYlBUocjPBvlxO9p
  email: 
    recipients:
    - vivek.40-cse-16@miejtammu.in
    on_success: always
    on_failure: always

before_install:
  - echo -e "machine github.com\n  login $GITHUB_TOKEN" >> ~/.netrc

script:
  - echo "skipping tests"	
#   - py.test -s # or py.test for Python versions 3.5 and below

deploy:
- provider: cloudfoundry
  username: apikey
  password: $apikey
  api: https://api.eu-gb.bluemix.net
  organization: vivek.40-cse-16@mietjammu.in
  space: dev
  manifest: manifest.yml
  on:
    branch: develop
